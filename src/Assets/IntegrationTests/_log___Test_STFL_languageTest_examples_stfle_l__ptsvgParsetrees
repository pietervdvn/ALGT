Test/STFL.language Test/examples.stfl e -l --ptsvg Parsetrees

Warning:
  While checking the totality of function "abstract":
    Following calls will fall through:
      abstract(("Bool" (type typeSet)))
      abstract(("Int" (type typeSet)))
  While checking the totality of function "codomains":
    Following calls will fall through:
      codomains("$")
      codomains((typeL typeSet))
  While checking the totality of function "domains":
    Following calls will fall through:
      domains("$")
      domains((typeL typeSet))
  
